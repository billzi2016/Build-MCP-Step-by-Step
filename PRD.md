# Build-MCP-Step-by-Step PRD

## 1. 项目名称

Build-MCP-Step-by-Step

## 2. 项目定位

这是一个面向招聘场景的技术作品集项目，不定位为“教学教程”，而定位为一套完整、可运行、可解释的 LLM Agent 与 MCP 系统设计与实现说明。

项目目标不是证明“会调用大模型 API”，而是系统展示以下能力：

- 理解大模型为什么能够表现出指令跟随、任务分解与工具使用能力
- 理解 system prompt、message roles、tool schema、上下文注入如何共同塑造模型行为
- 理解 Agent 并不是“聊天机器人升级版”，而是一个带有目标、状态、外部能力和执行闭环的运行时系统，也是模型与 MCP 能力层之间的实际调度者
- 理解 MCP 不只是“函数调用封装”，而是把模型可理解的信息结构与程序可执行的能力结构统一进同一套协议
- 能够基于本地 `Ollama + gpt-oss:120b` 构建一个最小但完整的 Agent Runtime 与 MCP 接入方案
- 能够把抽象原理、协议机制、工程边界、失败模式和可扩展性讲清楚

## 3. 项目背景

当前多数 LLM 项目展示停留在三种层次：

- Prompt 演示
- 单轮问答
- 零散的 function calling demo

这类展示通常能证明“模型能用”，但不能证明“系统可设计、可治理、可扩展”。

在真正的工程语境中，大模型并不是孤立工作的。它始终运行在一个由提示词、消息角色、工具约束、上下文资源、执行结果回填、错误处理和观测机制组成的系统中。所谓 Agent，也并不是模型自己“长出来”的能力，而是模型在一个受控运行时中不断完成“理解目标、选择动作、调用外部能力、读取反馈、继续推理”的过程。

MCP 的价值，正是在这一层出现。它试图标准化模型与外部工具、资源、任务模板之间的接口，让“模型理解什么”和“程序允许做什么”不再是散乱拼接，而成为一套统一、可复用、可组合的协议化能力系统。

但 MCP 本身并不是任务执行者。真正把任务拆开、决定何时读取 resource、何时调用 tool、何时停止循环并汇总结果的，是 Agent Runtime。换句话说，MCP 提供标准化能力面，Agent 负责在目标驱动下调度这些能力，模型则负责在当前上下文里做出下一步决策。

本项目将围绕这一主线展开，不写成“教人上手”的课程，而写成一套可用于招聘展示的架构型作品。

## 4. 项目目标

### 4.1 核心目标

构建一套由多个 `.ipynb` 组成的系统化内容与演示工程，从底层原理到本地可运行系统，完整展示以下主题：

- 大模型的指令跟随机制
- system prompt 与消息角色的控制作用
- tool calling 的本质、边界与执行闭环
- Agent 的定义、结构与运行方式
- Agent 如何调度 MCP 能力完成完整任务
- MCP 的动机、协议价值与系统设计
- 本地模型接入与能力验证
- 一个完整的端到端 Agent Runtime 示例
- 常见失败模式、约束设计与评估方法

### 4.2 展示目标

项目完成后，应当让审阅者得到以下判断：

- 作者理解 LLM 系统不是单纯 prompt engineering，而是一个运行时设计问题
- 作者理解 Agent、Tool Use、Context、MCP 之间的关系，而不是把它们当成孤立概念，并清楚知道是谁在调度谁
- 作者具备从协议、架构、接口到实现的完整视角
- 作者可以将抽象概念落实为本地可运行、可观测、可说明的工程原型

## 5. 目标读者

### 5.1 主要读者

- 技术招聘中的 HR
- 技术经理
- AI 工程师面试官
- 对 Agent / MCP / Tool Use 有基础认知的研发团队成员

### 5.2 读者预期

该项目不假设读者已经深入理解 MCP 或 Agent，但要求项目本身能让读者快速看出：

- 作者不是在复述概念
- 作者不是在做入门教程
- 作者对底层机制和工程结构有自己的组织能力

## 6. 非目标

以下内容不作为本项目重点：

- 面向零基础用户的入门科普
- 各类模型 API 的横向测评
- 云端部署、分布式服务和生产级高可用实现
- 花哨的 UI 展示
- 依赖大量第三方平台的复杂集成

本项目优先保证“机制清晰、结构完整、原理扎实、演示可运行”。

## 7. 核心观点

本项目围绕以下核心观点展开：

- 大模型的核心不是“会聊天”，而是在概率生成机制上形成了对指令、格式和任务结构的条件响应能力。
- system prompt 不只是提示语，而是模型行为边界和优先级排序的重要控制面。
- tool calling 不是额外外挂，而是把自然语言决策转化为结构化动作的关键桥梁。
- Agent 不是某个模型特性，而是一个围绕目标、状态、能力和反馈循环构建出来的运行时系统，也是整个外部能力编排的执行中枢。
- MCP 的价值不在于“又多一个协议”，而在于将可读上下文、可执行工具和可复用任务模板统一到一套标准接口中。
- MCP 解决的是“能力如何被标准化暴露”，Agent 解决的是“这些能力如何被任务化调度”。
- 一个可用的 Agent 系统，核心不在于工具数量，而在于边界清晰、上下文可控、调用可追踪、失败可恢复。

## 8. 核心概念定义

### 8.1 大模型的指令跟随

大模型本质上仍然是 next-token prediction 系统，但在大规模预训练、指令微调和偏好对齐之后，模型学会了把“任务描述”映射为特定风格、结构和行为模式的输出。所谓指令跟随，不是模型内部存在一套显式规则引擎，而是模型在条件上下文中学会了“什么样的输入，应当续写出什么样的响应结构”。

这意味着：

- 模型会受到提示顺序影响
- 模型会受到角色信息影响
- 模型会受到格式约束影响
- 模型会受到历史消息和工具结果影响

### 8.2 System Prompt

System prompt 是运行时最重要的高优先级控制信号之一。它决定的不是某一句回答，而是模型在整个会话中的身份、边界、目标倾向、禁令、输出格式偏好和决策风格。

在 Agent 体系中，system prompt 往往承担以下职责：

- 定义任务总目标
- 定义可用工具的使用原则
- 定义遇到不确定性时的处理方式
- 定义信息来源优先级
- 定义回复格式和行为约束

### 8.3 Tool Calling

Tool calling 的本质不是模型“真的去执行函数”，而是模型在给定 schema 的约束下，输出一个结构化调用意图。真正执行工具的是外围运行时。

一个完整的 tool calling 闭环包含：

- 模型读取当前任务和可用工具定义
- 模型判断是否需要借助外部能力
- 模型输出结构化工具调用请求
- 运行时执行对应工具
- 执行结果被回填为新的上下文
- 模型基于反馈继续推理并生成最终结果

### 8.4 Agent

Agent 不是单轮问答模型，也不是简单的“会调工具的聊天机器人”。Agent 更准确的定义应是：

一个围绕目标状态运行、能够在上下文中规划与选择动作、并借助外部工具和资源持续完成任务的推理执行系统。

Agent 通常至少包含以下元素：

- 目标
- 状态
- 可用能力
- 上下文记忆
- 决策机制
- 规划与动作选择策略
- 执行反馈闭环
- 工具与资源调度逻辑
- 终止条件
- 错误处理策略

如果把系统角色拆开来看：

- 模型负责在当前上下文中生成下一步最合理的输出
- Agent 负责把“下一步输出”解释成系统动作，并维护任务状态
- MCP 负责把外部能力标准化地暴露给 Agent 使用

因此，Agent 不是 MCP 的附属物，MCP 也不是 Agent 的替代品。二者分别解决“调度问题”和“能力暴露问题”。

### 8.5 Agent 与 MCP 的关系

在本项目语境里，MCP 必须放在 Agent Runtime 的执行链中理解，而不能单独理解为一个“工具集合”。

关系可以概括为：

- 模型输出决策意图
- Agent 判断当前意图属于回答、继续规划、读取资源还是调用工具
- Agent 通过 MCP 访问对应的 tool、resource 或 prompt
- MCP 返回标准化结果
- Agent 将结果写回上下文，驱动模型继续推理

也就是说，MCP 负责暴露能力，Agent 负责消费能力，模型负责在上下文中决定下一步最有可能的动作方向。没有 Agent，MCP 只是一层被动接口；没有 MCP，Agent 对外部能力的接入就会重新退回到零散、不可复用的自定义拼接。

### 8.6 MCP

MCP 可以理解为模型上下文与外部能力之间的标准化协议层。它并不是简单把“函数”和“文档”放到一起，而是把：

- 模型可读取的上下文资源
- 模型可调用的外部工具
- 模型可复用的任务模板

统一组织为标准接口，使不同客户端、模型宿主和能力提供方之间可以更稳定地协作。

## 9. 项目问题陈述

本项目试图回答以下问题：

1. 大模型为什么能够从“文本生成器”表现为“任务执行者”？
2. system prompt、message roles 和 tool schema 如何共同控制模型行为？
3. Agent 和普通聊天式 LLM 应用的根本差异是什么？
4. 为什么 Agent 才是 MCP 的实际调用者，而不是模型或协议本身直接“完成任务”？
5. 为什么 function calling 不足以构成一个清晰的外部能力体系？
6. MCP 为什么值得引入，它解决的到底是什么问题？
7. 如何在本地 `Ollama + gpt-oss:120b` 环境中构建一个最小但完整的 Agent Runtime？
8. 如何让这个系统既可演示，又能体现工程思维？

## 10. 解决方案概述

本项目将通过一组顺序编号的 Jupyter Notebooks 构建一条完整叙事链路：

- 先解释大模型为何表现出指令跟随与结构化行为
- 再解释 tool calling 的机制与限制
- 再引入 Agent 的定义与运行闭环
- 再引入 Agent 如何通过 Runtime 调度 MCP 能力
- 再引入 MCP 的必要性与协议设计思想
- 最后构建本地可运行的最小系统，展示从模型到工具、从资源到响应的完整链路

整套内容以“原理 -> 协议 -> 架构 -> 实现 -> 演示 -> 评估 -> 边界”为主线组织。

## 11. 内容形态

项目交付物以以下形式组织：

- 一个 `PRD.md`
- 多个顺序编号的 `.ipynb`
- 必要的本地示例代码和配置文件
- 最小 MCP server 示例
- 最小 Agent Runtime 示例
- 本地模型接入代码
- 端到端 trace 与案例输出

## 12. Notebook 结构设计

### 12.1 总体原则

所有 Notebook 应满足以下原则：

- 每一章都有明确的问题陈述
- 每一章都解释“为什么存在这一层”
- 每一章都尽量提供最小可验证代码或结构化示意
- 语言风格偏架构说明，不使用课堂化口吻
- 每一章既能独立阅读，又能串联成完整系统叙事

### 12.2 建议目录

#### 01_from_prompt_to_action.ipynb

主题：从文本生成到任务执行

目标：

- 解释大模型为什么会表现出指令跟随
- 解释从“回答问题”到“执行动作”的能力跃迁
- 建立后续 tool use 与 agent 运行时的理论起点

关键内容：

- next-token prediction 与行为表征
- 指令微调带来的分布偏移
- 输出格式约束如何改变模型决策空间
- 为什么模型看起来像“会规划”

#### 02_system_prompt_roles_and_control.ipynb

主题：system prompt、roles 与行为控制面

目标：

- 解释 system prompt 的作用层级
- 解释 user / assistant / tool / system 等角色如何改变上下文解释方式
- 解释为什么 Agent 行为本质上由“上下文编排”驱动

关键内容：

- role-based context composition
- 优先级与冲突
- 提示词边界与行为风格约束
- 长上下文中控制信号的衰减问题

#### 03_how_tool_calling_really_works.ipynb

主题：Tool Calling 的机制与执行闭环

目标：

- 解释 tool schema 的本质
- 解释模型如何选择调用工具
- 解释 tool result 回填后为什么会改变后续推理

关键内容：

- function name、description、parameters 的行为影响
- 工具选择是概率决策，不是规则匹配
- 结构化输出与执行系统解耦
- tool calling 的典型失败模式

#### 04_what_an_agent_actually_is.ipynb

主题：Agent 到底是什么

目标：

- 把 Agent 从营销概念还原为运行时结构
- 说明 Agent 与普通对话应用的本质区别
- 说明 Agent 的最小组成部分
- 说明 Agent 为什么是 MCP 的直接调用方

关键内容：

- goal
- state
- policy
- tools
- resources
- memory
- loop
- planning
- acting
- observing
- termination condition
- error recovery

#### 05_why_mcp_exists.ipynb

主题：为什么 MCP 值得存在

目标：

- 说明传统 function calling / prompt 拼接的痛点
- 说明 MCP 解决的是接口治理问题，而不仅是调用问题
- 说明 MCP 必须被放进 Agent Runtime 中才有完整意义

关键内容：

- 外部能力定义分散
- 文档与工具说明脱节
- 资源读取缺乏统一入口
- 多客户端适配复杂
- Agent 与 MCP 的职责边界
- MCP 作为统一能力描述层的价值

#### 06_mcp_protocol_design.ipynb

主题：MCP 的协议结构与概念模型

目标：

- 解释 tools、resources、prompts 各自角色
- 解释 host、client、server 如何协作
- 说明 MCP 的语义层与执行层关系

关键内容：

- MCP 架构角色划分
- tool / resource / prompt 的差异
- 为什么 resource 是关键能力
- 为什么 prompt 模板也应该协议化

#### 07_building_a_local_mcp_server.ipynb

主题：实现一个本地 MCP Server

目标：

- 通过最小可运行示例说明 MCP server 的构成
- 展示如何定义工具、资源、提示模板

关键内容：

- server 基础结构
- 工具 schema 设计
- resource 暴露方式
- prompt 模板定义
- 错误处理与输入校验

#### 08_connecting_ollama_and_gpt_oss_120b.ipynb

主题：本地模型接入与能力验证

目标：

- 说明为何选用 `Ollama + gpt-oss:120b`
- 说明本地推理如何接入 runtime
- 验证模型在指令遵循、结构化输出、工具决策上的表现

关键内容：

- 调用链路
- 输入输出格式
- 结构化调用约束
- 本地模型的稳定性、延迟和行为特点

#### 09_building_the_agent_runtime.ipynb

主题：构建端到端 Agent Runtime

目标：

- 把模型、system prompt、tool calling、MCP resource、执行反馈整合成一个闭环
- 展示一个完整任务如何被 runtime 驱动完成
- 展示 Agent 如何把 MCP 能力编排成任务执行链

关键内容：

- 任务入口
- 状态管理
- 决策循环
- resource read loop
- 工具调用执行
- prompt template selection
- 结果整合
- 终止条件

#### 10_case_study_hr_facing_demo.ipynb

主题：面向招聘展示的案例演示

目标：

- 用一个业务语境较强的案例展示整套系统
- 让非深度技术读者也能快速看懂价值

候选案例：

- 读取岗位 JD 与候选人信息，生成结构化能力映射
- 读取项目文档与代码说明，输出集成方案
- 读取本地知识资源并完成需求拆解与风险总结

#### 11_failure_modes_guardrails_and_evaluation.ipynb

主题：失败模式、约束与评估

目标：

- 展示作者不仅会做 demo，也理解系统边界
- 给出可观测、可复现的评估框架

关键内容：

- 工具误选
- 参数错误
- 幻觉使用资源
- Prompt Injection
- 上下文污染
- 重试与 fallback
- trace、logging、成功率与质量评估

#### 12_from_prototype_to_production.ipynb

主题：从演示系统到可扩展架构

目标：

- 说明这套设计如何走向工程化
- 展示架构抽象能力

关键内容：

- 多 server 组织方式
- 工具权限治理
- 资源版本化
- Prompt 版本化
- 可观测性与审计
- 成本、延迟与稳定性权衡

## 13. 系统设计范围

### 13.1 必须包含

- 本地 LLM 调用链路
- 最小 Agent Loop
- MCP server 原型
- tools / resources / prompts 三类能力
- Agent 通过 MCP 调度三类能力的完整闭环
- 结构化 trace 展示
- 至少一个端到端案例
- 失败模式与约束策略

### 13.2 可选增强

- 多轮任务执行
- 简单 memory 设计
- 简单 evaluation harness
- 工具调用日志可视化
- Resource 检索策略对比

## 14. 技术架构

### 14.1 运行环境

- 本地模型宿主：Ollama
- 主模型：gpt-oss:120b
- 开发形态：Jupyter Notebook
- 语言：Python
- 能力协议：MCP
- 运行方式：本地 agent runtime 驱动 MCP server 与模型协作

### 14.2 核心架构分层

系统建议拆分为四层：

- Model Layer
- Agent Runtime Layer
- Capability Layer
- Observation Layer

具体职责：

Model Layer：

- 负责接收拼装后的消息上下文
- 输出自然语言内容或结构化工具调用意图

Agent Runtime Layer：

- 负责 system prompt 注入
- 负责消息管理
- 负责目标推进与状态维护
- 负责规划下一步动作
- 负责工具调度
- 负责资源读取调度
- 负责 prompt 模板装配
- 负责结果回填
- 负责循环终止判断

Capability Layer：

- 通过 MCP 暴露 tools、resources、prompts
- 为 Agent 提供“可执行能力”和“可读取上下文”

Observation Layer：

- 记录消息轨迹
- 记录工具调用
- 记录错误
- 支撑调试与评估

## 15. Agent Runtime 设计原则

Agent Runtime 是本项目的核心实现对象，建议遵循以下原则：

- 模型只负责决策与生成，不直接承担执行
- Agent 负责把模型输出解释为系统动作，并决定是否访问 MCP 能力
- 工具执行必须由外部程序接管
- 上下文必须可追踪、可解释、可裁剪
- 调用链必须可以重放和审计
- 所有工具都需要清晰 schema 和失败输出
- 所有 MCP 资源读取都应进入统一的状态更新流程
- Agent 循环必须具备终止条件
- 失败必须可以分类，而不是一律归因于“模型不稳定”

## 16. MCP Server 设计原则

本项目中的 MCP server 不求复杂，但必须体现结构正确性。建议包含：

- 至少 2 到 3 个 tools
- 至少 2 类 resources
- 至少 1 到 2 个 prompts

设计要求：

- tool 要有清晰用途和输入结构
- resource 要体现“可被模型读取的上下文”
- prompt 要体现“可被复用的任务入口”
- 所有能力都应以“被 Agent 调用”为前提来描述输入输出契约
- 所有能力定义应能体现“文义说明与执行接口统一”的思想

## 17. 案例设计原则

案例不应显得像课堂作业，应满足以下标准：

- 有明确业务目标
- 能体现读取资源与调用工具的必要性
- 能体现模型并非直接凭空回答
- 能体现 Runtime 对流程的组织能力
- 最终输出具有“面向工作结果”的感觉

建议优先选择与招聘、文档分析、方案生成、需求拆解相关的案例，因为它们更适合 HR 理解。

## 18. 交互与叙事风格要求

整套内容的叙事风格要求如下：

- 整体形态应更接近一组有连续性的技术博客或深度文章，而不是上课大纲、教案、讲义或培训材料
- 每个 Notebook 都必须具备完整文章感，能独立成立，读起来像作者在展开一个判断，而不是在罗列知识点
- 不允许空洞复述概念，不允许把众所周知的定义堆在一起后直接结束，必须给出作者自己的结构化理解、边界判断和工程视角
- 不允许写成“看起来很全面，但读完没有获得任何新理解”的内容，必须确保每一节都有明确见解，而不是信息填充
- 关键章节必须回答“为什么是这样设计，而不是别的设计”，不能只写“它是什么”
- 叙事上要有论点推进感，而不是目录展开感；读者应当感受到作者在建立一个完整观点，而不是在完成一份课程任务
- 允许解释概念，但解释的目标是支撑观点，不是为了凑齐教学链路
- 不使用“接下来我们学习”
- 不使用“这个非常简单”
- 不使用“只需要这样做”
- 不写成老师口吻
- 不刻意讨好读者
- 优先使用架构、机制、边界、约束、运行时等术语
- 每一节都说明“为什么这一层存在”
- 强调系统观，而不是零散技巧

## 19. 成功标准

如果项目达到以下标准，则视为成功：

- 读者能清楚理解 LLM、Tool Calling、Agent、MCP 之间的关系
- 读者能清楚理解 Agent 是 MCP 的调度者，而不是把 MCP 误解成“自动执行系统”
- 读者能看到一个完整而不是碎片化的系统
- Notebook 的组织结构能形成由浅入深但不教学化的叙事
- 本地 `Ollama + gpt-oss:120b` 的接入是实际可运行的
- MCP server 示例不是摆设，而是被真实纳入 agent loop
- 项目展示出明确的工程判断力，而不只是概念堆砌

## 20. 风险与挑战

### 20.1 模型层风险

- 本地模型对工具调用格式的遵循可能不稳定
- 长上下文下 system prompt 的控制力可能下降
- 不同任务下工具选择可能出现偏差

### 20.2 工程层风险

- Notebook 内容可能过于分散
- MCP 演示如果过于简化，容易显得像概念样板
- 案例如果过于“教学化”，会削弱作品集观感

### 20.3 叙事层风险

- 如果过多解释基础概念，会像教程
- 如果过多堆砌协议细节，会让 HR 难以抓住重点
- 如果缺少端到端案例，整套内容会显得停留在原理层

## 21. 应对策略

- 用“问题驱动”组织每个 Notebook，而不是“知识点罗列”
- 用“最小可运行系统”统一所有内容
- 用一个强案例贯穿演示
- 用失败模式与评估章节体现成熟度
- 用清晰结构替代冗长说教

## 22. 交付物清单

最终建议交付以下文件：

- `PRD.md`
- `01_from_prompt_to_action.ipynb`
- `02_system_prompt_roles_and_control.ipynb`
- `03_how_tool_calling_really_works.ipynb`
- `04_what_an_agent_actually_is.ipynb`
- `05_why_mcp_exists.ipynb`
- `06_mcp_protocol_design.ipynb`
- `07_building_a_local_mcp_server.ipynb`
- `08_connecting_ollama_and_gpt_oss_120b.ipynb`
- `09_building_the_agent_runtime.ipynb`
- `10_case_study_hr_facing_demo.ipynb`
- `11_failure_modes_guardrails_and_evaluation.ipynb`
- `12_from_prototype_to_production.ipynb`

## 23. 下一阶段工作建议

在 PRD 确认后，下一阶段工作按以下顺序推进：

1. 先定每个 Notebook 的章节结构、图示需求和代码粒度
2. 再统一定义最小 MCP server 的能力边界
3. 再搭建本地 runtime 与 Ollama 接入代码
4. 最后把案例、评估与风险章节补齐

## 24. 一句话总结

这个项目不是“讲怎么学 MCP”，而是用一套完整的 Notebook 系统，证明作者理解大模型、工具调用、Agent Runtime 与 MCP 协议之间的关系，并能把这种理解落实为一个本地可运行、结构清晰、可解释、可扩展的工程原型。
